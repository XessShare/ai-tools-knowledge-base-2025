╔══════════════════════════════════════════════════════════════════════════════╗
║                    PROXMOX SERVER COMPREHENSIVE SCAN REPORT                  ║
╚══════════════════════════════════════════════════════════════════════════════╝

Scan Date: {{ ansible_date_time.iso8601 }}
Host: {{ inventory_hostname }} ({{ ansible_host }})

┌──────────────────────────────────────────────────────────────────────────────┐
│ SYSTEM INFORMATION                                                           │
└──────────────────────────────────────────────────────────────────────────────┘
Hostname: {{ report_data.hostname }}
System: {{ report_data.uname }}
Uptime: {{ report_data.uptime }}

CPU Information:
{{ report_data.cpu | join('\n') }}

Memory Information:
{{ report_data.memory | join('\n') }}

Disk Usage:
{{ report_data.disk | join('\n') }}

┌──────────────────────────────────────────────────────────────────────────────┐
│ PROXMOX INFORMATION                                                          │
└──────────────────────────────────────────────────────────────────────────────┘
Version:
{{ report_data.pve_version | join('\n') }}

Cluster Status:
{{ report_data.cluster_status | join('\n') }}

┌──────────────────────────────────────────────────────────────────────────────┐
│ STORAGE                                                                      │
└──────────────────────────────────────────────────────────────────────────────┘
{{ report_data.storage | join('\n') }}

┌──────────────────────────────────────────────────────────────────────────────┐
│ VIRTUAL MACHINES                                                             │
└──────────────────────────────────────────────────────────────────────────────┘
{{ report_data.vms | join('\n') }}

┌──────────────────────────────────────────────────────────────────────────────┐
│ CONTAINERS                                                                   │
└──────────────────────────────────────────────────────────────────────────────┘
{{ report_data.containers | join('\n') }}

┌──────────────────────────────────────────────────────────────────────────────┐
│ NETWORK CONFIGURATION                                                        │
└──────────────────────────────────────────────────────────────────────────────┘
{{ report_data.network | join('\n') }}

┌──────────────────────────────────────────────────────────────────────────────┐
│ EXISTING HOMELAB SETUP                                                       │
└──────────────────────────────────────────────────────────────────────────────┘
Homelab Directory Exists: {{ report_data.homelab_exists }}

╚══════════════════════════════════════════════════════════════════════════════╝
End of Report
